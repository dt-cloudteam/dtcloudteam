apiVersion: v1
kind: Service
metadata:
  name: salihtest-website-loadbalancer-service
  namespace: salihtest
spec:
  type: NodePort
  selector:
    app: salihtest-website
  ports:
  - port: 8080
    targetPort: 80